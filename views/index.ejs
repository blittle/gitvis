<!DOCTYPE html>
<html ng-app='gitvis'>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.2.0/pure-min.css">
    <link rel="stylesheet" href="http://yui.github.io/skinbuilder/?mode=pureindex.html?opt=mine,3355BA,ffffff,1,1,10,1.5&h=0,-30,60&n=0,-30,75&l=0,-30,80&b=0,-30,90&mode=pure">
    <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.0/css/font-awesome.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.0.7/angular.min.js"></script>
    <script src="http://code.angularjs.org/1.1.5/angular-resource.js"></script>
    <script>
            var gitvis = {
                repos: <%-JSON.stringify(repos)%>
            };
    </script>
    <script src="/scripts/app.js"></script>
  </head>
  <body>
        <h1 class="mainHead l-centered"><%= title %></h1>
        <div class="repos" ng-controller="ReposController">
           <table class="repoTable pure-table pure-table-horizontal">
                <thead>
                    <th>Repository</th>
                    <th></th>
                </thead>
                <tbody>
                    <tr ng-repeat="repo in repos">
                        <td ng-click="openRepo(repo.id)">{{repo.name}}</td>
                        <td><i ng-click="deleteRepo(repo.id)" class="icon-trash"></i></td>
                    </tr>
                </tbody>
           </table>
           <form ng-submit="createNewRepo(newRepoName)" class="addRepoForm pure-form">
               <input type="text" class="pure-input-rounded" placeholder="Github Repo" value="{{newRepoName}}">
               <button type="submit" class="pure-button pure-button-primary">Add Repo</button>
           </form>
           <div>{{loadedRepo.history}}</div>
        </div>
  </body>
</html>
